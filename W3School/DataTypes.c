#include <stdio.h>

int main() {
    int studentID = 2025331540;
    float score = 3.75;
    char grade = 'A';

    printf("StudentID is %d", studentID);
    printf("\nScore is %.2f", score);
    printf("\nGrade is %c", grade);
    return 0;
}
